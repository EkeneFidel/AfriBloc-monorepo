export interface FireblocksConfig {
    apiKey: string;
    secretKey: string;
    baseUrl: string;
    vaultAccountId?: string;
}
export interface CurrencyConfig {
    apiKey: string;
    baseUrl: string;
}
